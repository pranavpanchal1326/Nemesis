/** §E14.4 — the developer portal: keys, webhooks, usage, the deprecation clock. */
export default function DevLayout({ children }: { children: React.ReactNode }) {
  return <main data-surface="developers">{children}</main>;
}
